---
deployment:
  tasks:
    - export RSYNCPATH="$(which rsync)"
    - export DEPLOYPATH=/home/ylrdxapi/domains/rvhsgamejam.x10.mx/public_html
    - $RSYNCPATH -rt . $DEPLOYPATH
    # - /bin/cp index.php $DEPLOYPATH
    # - /bin/cp -r static $DEPLOYPATH
    # - /bin/cp -r templates $DEPLOYPATH
    # - /bin/cp -r backend $DEPLOYPATH
